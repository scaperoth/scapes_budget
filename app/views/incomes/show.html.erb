<%- model_class = Income -%>
<% content_for :head do %>
<%=t '.title', default: model_class %>
<br/>
<small><%= @income.name %></small>
<% end %>
<section class="page-section resource-show">
    <div class="uk-width-1-1 uk-width-large-1-3 uk-width-xlarge-1-4 uk-container-center ">
        <div class="fieldset">
            <dl>
                <dt class="label label-default"><%= model_class.human_attribute_name(:frequency) %></dt>
                <dd>
                    <pre class="prettyprint"><%= @income.frequency.name.capitalize %></pre>
                </dd>
                <dt class="label label-default"><%= model_class.human_attribute_name(:name) %></dt>
                <dd>
                    <pre class="prettyprint"><%= @income.name %></pre>
                </dd>
                <dt class="label label-default"><%= model_class.human_attribute_name(:amount) %></dt>
                <dd>
                    <pre class="prettyprint"><%= @income.amount %></pre>
                </dd>
                <dt class="label label-default"><%= model_class.human_attribute_name(:notes) %></dt>
                <dd>
                    <pre class="prettyprint"><%= @income.notes %></pre>
                </dd>
            </dl>
        </div>

        <div class="form-group">
            <%= link_to t('.back', :default => t("helpers.links.back")),
              edit_budget_path(@budget), :class => 'uk-button uk-button-default'  %>
            <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_income_path(@income), :class => 'uk-button uk-button-default' %>
            <%= link_to t('.destroy', :default => t("helpers.links.destroy")), income_path(@income), :method => 'delete', :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'uk-button
            uk-button-danger' %>
        </div>
    </div>
</div>