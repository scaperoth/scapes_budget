<h1>Listing categories_expenses</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories_expenses.each do |categories_expense| %>
      <tr>
        <td><%= link_to 'Show', categories_expense %></td>
        <td><%= link_to 'Edit', edit_categories_expense_path(categories_expense) %></td>
        <td><%= link_to 'Destroy', categories_expense, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Categories expense', new_categories_expense_path %>
